[workspace]
members = [
    "crates/amplifier-core",
    "crates/amplifier-guest",
    "bindings/python",
    "bindings/node",
]
resolver = "2"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
