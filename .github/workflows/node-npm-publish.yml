name: Publish Node.js Package

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    name: Build — ${{ matrix.settings.target }}
    runs-on: ${{ matrix.settings.host }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        settings:
          - host: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - host: ubuntu-latest
            target: aarch64-unknown-linux-gnu
          - host: macos-latest
            target: aarch64-apple-darwin
          - host: macos-latest
            target: x86_64-apple-darwin
          - host: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.settings.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"
          cache-on-failure: true
          key: node-${{ matrix.settings.target }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install cross-compilation dependencies (aarch64-unknown-linux-gnu)
        if: matrix.settings.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install Node.js dependencies
        working-directory: bindings/node
        run: npm install

      - name: Build
        working-directory: bindings/node
        run: npx napi build --release --platform --target ${{ matrix.settings.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Upload native binding
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.settings.target }}
          path: bindings/node/*.node
          if-no-files-found: error

      # index.js and index.d.ts are gitignored but generated by napi build.
      # Upload from one platform only — they are identical across all targets.
      - name: Upload JS bindings (index.js + index.d.ts)
        if: matrix.settings.target == 'x86_64-unknown-linux-gnu'
        uses: actions/upload-artifact@v4
        with:
          name: js-bindings
          path: |
            bindings/node/index.js
            bindings/node/index.d.ts
          if-no-files-found: error

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write  # required for --provenance (OIDC signing of attestations only — does NOT replace NPM_TOKEN auth)

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'

      - name: Install Node.js dependencies
        working-directory: bindings/node
        run: npm install

      - name: Download native bindings
        uses: actions/download-artifact@v4
        with:
          pattern: bindings-*
          merge-multiple: true
          path: bindings/node/artifacts/

      - name: Download JS bindings
        uses: actions/download-artifact@v4
        with:
          name: js-bindings
          path: bindings/node/

      - name: List artifacts
        run: |
          echo "=== .node files ==="
          ls -lh bindings/node/artifacts/
          echo "=== JS bindings ==="
          ls -lh bindings/node/index.js bindings/node/index.d.ts

      - name: Create npm platform package directories
        working-directory: bindings/node
        run: npx napi create-npm-dir -t .

      - name: Move native bindings into platform packages
        working-directory: bindings/node
        run: npx napi artifacts --dir artifacts --dist npm

      - name: List platform packages
        working-directory: bindings/node
        run: find npm -type f | sort

      - name: Inject optionalDependencies into main package.json
        working-directory: bindings/node
        run: |
          node -e "
          const fs = require('fs');
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          const v = pkg.version;
          const n = pkg.name;
          pkg.optionalDependencies = {
            [n + '-darwin-arm64']:    v,
            [n + '-darwin-x64']:      v,
            [n + '-linux-arm64-gnu']: v,
            [n + '-linux-x64-gnu']:   v,
            [n + '-win32-x64-msvc']:  v,
          };
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          console.log('optionalDependencies injected:', JSON.stringify(pkg.optionalDependencies, null, 2));
          "

      - name: Publish platform packages
        working-directory: bindings/node
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          shopt -s nullglob
          for pkg_dir in npm/*/; do
            echo "Publishing ${pkg_dir} ..."
            npm publish "${pkg_dir}" --access public
          done

      - name: Publish main package
        working-directory: bindings/node
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --provenance
